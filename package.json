{
  "name": "models-pipeline-aws",
  "private": true,
  "description": "NovaRetail monorepo: desde la raíz usa npm run verify y npm run install:all.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && node scripts/repair-npm-bin.cjs",
    "repair:bin": "node scripts/repair-npm-bin.cjs",
    "verify": "node scripts/repair-npm-bin.cjs && npm run build --prefix backend && npm run build:dev --prefix frontend",
    "test:backend": "npm test --prefix backend",
    "test:e2e": "npm run test:e2e --prefix backend",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  }
}
